# Port the server listens on (frontend VITE_BACKEND_URL should match, e.g. http://localhost:3001)
PORT=3001

# Optional: extra CORS origins, comma-separated (e.g. http://localhost:5173 for local dev)
# CORS_ORIGIN=http://localhost:5173
