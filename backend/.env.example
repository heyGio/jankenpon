# Port the server listens on (frontend VITE_BACKEND_URL should match, e.g. http://localhost:3001)
PORT=3001

# Optional: restrict CORS to these origins (comma-separated). If unset, all origins are allowed.
# CORS_ORIGIN=https://jankenpon.nguyenvanloc.com
